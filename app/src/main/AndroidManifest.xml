<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aindri.hemo">

<uses-permission android:name="android.permission.CALL_PHONE"></uses-permission>
<supports-screens android:anyDensity="true"
    android:largeScreens="true"
    android:smallScreens="true"
    android:xlargeScreens="true"
    android:normalScreens="true"></supports-screens>
    <application
        android:allowBackup="true"
        android:icon="@drawable/hemologo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/hemologo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".VerifyDetails"></activity>
        <activity android:name=".OTPverify" />
        <activity android:name=".MyProfileActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".EditProfileActivity" />
        <activity android:name=".FeedbackActivity" />
        <activity android:name=".SearchDonorActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ForgetPasswordActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBcZ9pEmnuvlsoAib0k8eq_5ZyfTSawXUo" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>